[package]
name = "sentiric-sip-signaling-service"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
tonic = "0.11"
prost = "0.12"
lapin = "2.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
config = "0.14"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["json", "env-filter"] }
rand = "0.8"
tokio-util = "0.7"

# YENİ: Hazır derlenmiş gRPC kodlarını içeren merkezi kütüphanemizi ekliyoruz.
[dependencies.sentiric-contracts]
git = "https://github.com/sentiric/sentiric-contracts.git"
tag = "v0.4.0"

# build.rs'i sileceğimiz için bu bölüme artık ihtiyaç yok.
# [build-dependencies]
# tonic-build = "0.11"